---
general:
  name: "root.yaml"
  version: "0.0.1"
  metadata:
    author: "Sam Caldwell"
    created_on: "2025-06-03"

patterns:
  - pattern: "rfc1123addresses"
    include: "patterns/rfc-1123.yaml"
  - pattern: "simple_identifier"
    regex: "[a-zA-Z][a-zA-Z0-9\\_]{1,64}"

assertions:
  # We can create a chain of all YAML assertion manifests to form a chain for analysis.
  - include: "hostname.yaml"
  - include: "ntp-checks.yaml"
  #- include: "dns-checks.yaml"
