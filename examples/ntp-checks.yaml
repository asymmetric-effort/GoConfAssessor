---
assertions:
  - name: "ntp-checks"
    items:
      - label: "<string: unique within this group>"
        parser: "<string: cisco-ios.yaml | juniper | arista | yaml | json | ini | text>"
        applies_to:
          - "<string: tag1>"
          - "<string: tag2>"
        statement: "<string: description of the standard being asserted>"
        expected:
          type: string
          value: "10.10.10.10"
        weight: 100
        source:
          path: "ntp.servers""
          pattern: "^ntp-server\ : .+$"
        operator: "<matches|contains|equals>"
