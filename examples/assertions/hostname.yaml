# we include a succession of YAML files
# but only those with a matching applies-to will be applied to a given target device.
- include: "examples/assertions/hostname/cisco-ios.yaml"
- include: "examples/assertions/hostname/juniper.yaml"
